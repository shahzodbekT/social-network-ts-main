import Modal from 'react-modal'
import { useForm } from 'react-hook-form'
import { yupResolver } from '@hookform/resolvers/yup'

export const AddNewPost = () => {
    return (
        <div>
            Тут будет добавление поста
        </div>
    )
}